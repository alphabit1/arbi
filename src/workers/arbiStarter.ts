// import Arbi from './../Arbi';
// import { expose } from 'threads/worker';

// const arbiStarter = {
//   start(coins: string[], fee: number, penta: boolean) {
//     let arbi = new Arbi(coins, fee);
//     arbi.init(penta).then(() => {
//       arbi.start();
//     });
//   }
// };

// export type ArbiStarter = typeof arbiStarter;

// expose(arbiStarter);
